<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Pay">

    <select id="selectKeyByAppid" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT `key` FROM `pay_wx_key` WHERE appid = #{appid}
    </select>

</mapper>